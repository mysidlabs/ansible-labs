events { }

http {
  upstream mysidlabs {
  {% for host in groups['tier_web'] %}
    server {{ hostvars[host]['ansible_eth0']['ipv4']['address'] }};
  {% endfor %}
  }

  server {
    listen 80;
    location / {
      proxy_pass http://mysidlabs;
    }
  }
}
